UI integration 15:
- On MainPage changed transition when clicking "Register" > navigate to WaitingForDevicePage
- On RegsiterDevice page:
	- If user chooses an already used device name, a warning popup opens, when closing the popup > navigate to RegsiterDevice page

TODO : 
- Find a way to add "divider" Image without bugging the UI
- Rename apdu.cs and its containing classes
- Add shade around subgrid
