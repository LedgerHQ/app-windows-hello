UI integration 17:
- Added Cancel button on Waiting for device page (only visible if number of registered devices > 0 ), added PointerEntered PointerExited Tapped callbacks for that button (tapped > navigate to device list)

TODO : 
- Rename apdu.cs and its containing classes
- Add shade around subgrid
- Border outside mainpage is white, find a way to make it grey
- Add multilingual support
