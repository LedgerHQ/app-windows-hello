New function writeConnectedRegisteredDevices(), this function lists all the connected registered devices, and writes then to a txt file
Code optimisation
Remove commented old code

(commit before new strategy for dLock : add bool to device config data to indicate if device was used for last login)