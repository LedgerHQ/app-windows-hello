Placed every calls to writeConnectedRegisteredDevices() and readConnectedRegisteredDevices() in try/catch/finally instructions to recover the app when txt file was not read/written properly
Add call to writeConnectedRegisteredDevices() when authentication is triggered by button press on locked screen

TODO:
- split the BGtask with two different tasks one triggered by auth trigger, and onother one triggered by device add/remove/update event